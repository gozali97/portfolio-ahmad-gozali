// Header component

export default function Header() {
    // This component is now minimal since we've moved navigation to the Navigation component
    // It serves as a placeholder for potential metadata or SEO elements
    return null;
}